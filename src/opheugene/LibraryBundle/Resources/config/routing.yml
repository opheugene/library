index:
    path:     /
    defaults: { _controller: opheugeneLibraryBundle:Default:index }

new:
    path:     /new
    defaults: { _controller: opheugeneLibraryBundle:Default:new }

update:
    path:     /book/{id}
    defaults: { _controller: opheugeneLibraryBundle:Default:update }
    
hello:
    path:     /hello/{name}
    defaults: { _controller: opheugeneLibraryBundle:Default:hello }
